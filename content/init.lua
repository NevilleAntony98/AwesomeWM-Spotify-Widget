-- Return UI Table
return {
	album_cover = require("widget.spotify.content.album-cover"),
	song_info = require("widget.spotify.content.song-info"),
	media_buttons = require("widget.spotify.content.media-buttons")
}